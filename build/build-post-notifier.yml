trigger:
  - master

resources:
- repo: self

variables:
  DOCKER_BUILDKIT: 1

stages:
- template: templates/stages/build-test-publish-dotnet-docker.yml
  parameters:
    componentName: post-notifier
    dockerfilePath: '$(Build.SourcesDirectory)/src/TaylorBot.Net/Program.PostNotifier/Dockerfile'
    dockerBuildContext: '$(Build.SourcesDirectory)/src/TaylorBot.Net'
